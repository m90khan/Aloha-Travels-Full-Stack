extends base
    
block content
    .section-tours.u-mt-20
        .tour
            .tour-heading
                img(src='/img/svg/destination.svg' alt='destination')
                h1.tour-heading__secondary Blogs
            .tour-packages
                each blog in blogs
                    .package
                        .package-header
                            picture
                                img.lazyload.package-header__image(src=`/img/posts/${blog.photo}`, alt=`${blog.title}`)
                            .package-header-wishicon
                                a#addtowishlist
                                    svg.package-header-wishicon__wishlist.package-header__wishlist--active
                                        use(xlink:href='/img/icons/sprite.svg#icon-heart')
                            h6.package-header__price= blog.author
                        .package-content
                            h4.secondary-heading= blog.title
                            p.desc-text.u-mb-2= blog.body
                        //- .package-card
                        //-     .package-card__data
                        //-             svg.package-card__data-icon
                        //-                 use(xlink:href='/img/icons.svg#icon-map-pin')
                        //-             p= tour.startLocation.description
                        //-     .package-card__data
                        //-             svg.package-card__data-icon
                        //-                 use(xlink:href='/img/icons.svg#icon-calendar')
                        //-             p= tour.startDates[0].toLocaleString('en-us', {month: 'long', year: 'numeric'})
                        //-     .package-card__data
                        //-             svg.package-card__data-icon
                        //-                 use(xlink:href='/img/icons.svg#icon-flag')
                        //-             p= `${tour.locations.length} stops`
                        //-     .package-card__data
                        //-             svg.package-card__data-icon
                        //-                 use(xlink:href='/img/icons.svg#icon-user')
                        //-             p= `${tour.maxGroupSize} people`
                        .package-cart
                            //- h6.package-cart-rating= `${tour.ratingsAverage} rating (${tour.ratingsQuantity})`
                            a.btn.btn--tertiary(href=`/blog/${blog.slug}`) Details 
    include components/_subscribe
